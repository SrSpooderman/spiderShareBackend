[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
markers =
    unit: pure unit tests without database, network, or FastAPI TestClient
    http: FastAPI route tests using TestClient and dependency overrides
    integration: tests that use persistence, migrations, Docker, or external services
